import { h } from 'preact';

const Header = () => {
  return (
    <header className="header">
      <nav className="nav">
        <a href="/" className="nav-link">Home</a>
      </nav>
    </header>
  );
};

export default Header; 